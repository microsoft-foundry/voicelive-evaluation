# VoiceLive Audio Processor - Container App
# Python 3.11+

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Azure SDKs
azure-identity>=1.25.3
azure-ai-voicelive==1.2.0b4  # Pinned to last working beta: 1.2.0+ removes AgentSessionConfig (needs AgentConfig migration before unpinning)
azure-ai-projects>=1.0.0b7   # Container App uses 1.x API surface; bump to 2.x tracked separately
azure-storage-blob>=12.29.0
azure-data-tables>=12.5.0
azure-core>=1.41.0

# Monitoring and Telemetry
azure-monitor-opentelemetry>=1.8.8
opentelemetry-instrumentation-fastapi>=0.49b0

# Audio processing
numpy>=2.4.6

# Async utilities
aiofiles>=23.2.0
aiohttp>=3.14.0

# Environment and utilities
python-dotenv>=1.2.2
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging
structlog>=24.1.0
