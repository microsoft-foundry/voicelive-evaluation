gender_analysis:
  class: audio_evals.prompt.base.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "{{WavPath}}"
          - type: text
            value: "listen the audio and judge the gender of the speaker, the answer must be one of [female, male], answer without explain"
